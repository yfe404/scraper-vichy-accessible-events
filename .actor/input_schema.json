{
    "title": "Actor input",
    "description": "Configuration for the Vichy-Tourisme events crawler.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "maxEvents": {
            "title": "Max number of events to fetch",
            "type": "integer",
            "description": "The crawler stops after it has processed this many events.",
            "editor": "number",
            "default": 10,
            "prefill": 42,
            "minimum": 1
        },
        "maxRequestsPerCrawl": {
            "title": "Max Requests per Crawl",
            "type": "integer",
            "description": "Maximum number of requests that can be made by this crawler.",
            "default": 100
        }
    },
    "required": ["maxEvents", "maxRequestsPerCrawl"]
}
